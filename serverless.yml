service: bus-lviv-bot
provider:
  name: aws
  runtime: nodejs8.10
  stage: production

functions:
  bot:
    handler: serverless_bot.handler
    environment:
      BOT_TOKEN: ${ssm:bus_lviv_bot_token}
    events:
      - http:
          path: bot
          method: any